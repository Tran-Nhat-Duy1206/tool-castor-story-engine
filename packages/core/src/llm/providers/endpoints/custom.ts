// LLM provider configuration and endpoints.
import type { ProviderEndpoint } from "../types.js";

// LLM provider configuration and endpoints.
export const CUSTOM: ProviderEndpoint = {
  id: "custom",
  label: "",
  api: "openai-completions",
  baseUrl: "",
  models: [],
};
